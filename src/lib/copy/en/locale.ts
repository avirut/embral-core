// The BCP-47 locale this catalog is written in. Passed to plural() and any
// other Intl-backed formatting so the choice is explicit, never the system
// default (docs/copy.md). A second locale's directory exports its own.
export const locale = 'en';
